{
  "require": "ts-node/register,source-map-support/register",
  "watch-extensions": "ts",
  "watch-files": ["src/**/*.ts", "test/unit/*test.ts"],
  "recursive": true,
  "reporter": "spec",
  "timeout": 5000
}
